# Statix — GDB init before attaching to QEMU (i386 stub).
# Loaded by the Makefile connect-gdb targets (see top-level Makefile).

set architecture i386
set pagination off
set print pretty on
set disassemble-flavor intel
